internal_port = 8080
runtime = "python"

# Committed Dockerfile: exec-form uvicorn so Previewly can prepend opentelemetry-instrument when enabled.
# App entry is backend.main:app (manual OpenTelemetry in backend/telemetry.py).
dockerfile = "Dockerfile"

[env]
LOG_LEVEL = "info"
