internal_port = 8080
runtime = "python"

# Committed Dockerfile: exec-form `uvicorn` so Previewly's OTLP ENTRYPOINT bootstrap can wrap it with
# `opentelemetry-instrument` without nesting another wrapper. OTEL deps live in requirements.txt.
dockerfile = "Dockerfile"

[env]
LOG_LEVEL = "info"
